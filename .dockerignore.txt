# ── OCML-DI .dockerignore ───────────────────────────────────────
# Excludes files that should not be copied into the Docker image

# Virtual environment — never copy into image
.venv/
venv/
env/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Git
.git/
.gitignore

# VS Code
.vscode/

# Environment files with secrets
.env

# Test files
pytest_cache/
.pytest_cache/
*.test.py

# Documentation and design (not needed at runtime)
docs/
design/
security/
*.md

# Frontend (served separately)
frontend/

# Database — use volume mount instead
ocml_di.db

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
